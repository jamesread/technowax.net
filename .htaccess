# Apache document root should be public/ (Laravel).
# If the vhost points at the repo root instead, forward requests into public/.

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
